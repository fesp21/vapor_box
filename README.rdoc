== Subscription Service Backbone/Rails

Backbone/Rails app with complete checkout system using Stripe. Subscription service with add-ons with very specific requirements in terms of number/type of items in cart. Got rejected by Stripe so switched over in production to Authorize.net/active merchant