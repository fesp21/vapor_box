    <% _.each(collection, function(item) { %>
    <ul>
        <li><%= item.name %></li>
        <% if (item.cost) {%>
        <li><%= item.cost %></li>
        <% } %>

    </ul>
    <% }); %>
    <button class="back">Back</button>
    <button class="continue">Continue</button>
