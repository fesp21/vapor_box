    <% _.each(collection, function(item) { %>
    <ul>
        <li><%= item.name %></li>
        <li><%= item.cost %></li>

    </ul>
    <% }); %>

    <button class="form-continue">Continue to Form</button>

